# Telegram Bot Token (required)
# Get it from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Your Telegram Chat ID (required)
# Get it from @userinfobot on Telegram
ALLOWED_CHAT_ID=your_chat_id_here

# Project directory for Claude to work on (optional, defaults to current directory)
PROJECT_DIR=/path/to/your/project

# Max turns per Claude query (optional, default: 30)
# One turn = one Claude API round-trip. Complex tasks need more turns.
# If you get "Unknown error" on long tasks, increase this value.
MAX_TURNS=30

# Google Cloud Speech-to-Text (optional, for voice message support)
# Path to your Google Cloud service account key JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
